[package]
name = "simple-guard"
version = "0.1.0"
edition = "2021"

[dependencies]
ckb-std = { version = "1.1.0", features = ["build-with-clang"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = true
